# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Prova.p1()
POST    /p2                         controllers.Prova.p2()
GET     /p2                         controllers.Prova.p2()
GET     /p4                         controllers.Prova.p4()

# Widgets
#GET     /widgets                    controllers.WidgetController.listWidgets
#POST    /widgets                    controllers.WidgetController.createWidget

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
